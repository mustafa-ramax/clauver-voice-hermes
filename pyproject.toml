[project]
name = "clauver"
version = "0.1.0"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["dispatch_api"]

[tool.setuptools.packages.find]
include = ["mcp_bridge*"]
